/// <reference path="index.d.ts" />

module leisuresoft {
    "use strict";
    var module = angular.module("sampleapp", []);

    module.config([
        () => {
            
        }]);

    module.run([
        () => {
           
        }]);
}
